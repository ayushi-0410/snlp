<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
	http://www.springframework.org/schema/beans/spring-beans.xsd 
	http://www.springframework.org/schema/security 
	https://www.springframework.org/schema/security/spring-security.xsd">

	<http auto-config="true" use-expressions="true">
	
	<!-- Allow these pages without authentication -->
	<intercept-url pattern="/" access="permitAll"/>
    <intercept-url pattern="/login" access="permitAll"/>
    <intercept-url pattern="/register" access="permitAll"/>
    <intercept-url pattern="/resources/**" access="permitAll"/>
    <intercept-url pattern="/public/**" access="permitAll"/>
    <intercept-url pattern="/index" access="permitAll"/>
    <intercept-url pattern="/signUp" access="permitAll"/>
    <intercept-url pattern="/logInAsGuest" access="permitAll"/>
    
    

		<intercept-url pattern="/**" access="hasAuthority('USER')" />
		
		<intercept-url pattern="/adminprofile" access="hasAuthority('ADMIN')" />
	
	<form-login 
		    login-page="/login" 
			login-processing-url="/login" 
		    default-target-url="/profile"
			authentication-failure-url="/login?error" 
			username-parameter="email"
			password-parameter="password" />
		<logout logout-success-url="/index?logout"  />
		<access-denied-handler error-page="/403" />
		<csrf disabled="true"/>        
	</http>
	
	<beans:bean id="passwordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>

	<authentication-manager>
		<authentication-provider>
		<password-encoder ref="passwordEncoder"/>
			<jdbc-user-service data-source-ref="dataSource"
				users-by-username-query="select EMAIL,PASSWORD,ENABLED from userdetails where EMAIL=?"
				authorities-by-username-query="select EMAIL,AUTHORITY from userdetails where EMAIL=?" />
		</authentication-provider>
	</authentication-manager>
	
	
</beans:beans>